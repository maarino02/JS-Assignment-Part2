https://maarino02.github.io/JS-Assignment-Part2/

Fixes:

- Add some indication of the active operator, since it does not display.
- Adding 0 multiple times after a decimal does not display the extra 0 values until a non-0 value is added.
